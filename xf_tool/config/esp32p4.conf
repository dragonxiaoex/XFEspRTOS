# ESP32-P4 项目的公共系统配置，产品 sdkconfig.conf 可覆盖同名选项。

# RTOS 与终端支持，沿用当前项目的统一设置。
CONFIG_FREERTOS_HZ=1000
CONFIG_FREERTOS_IN_IRAM=y
CONFIG_VFS_SUPPORT_TERMIOS=y

# ESP32-P4 共用分区表，路径相对于 xf_tool。
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="config/esp32p4.csv"
